name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build and deploy site
      run: |
        bundle exec jekyll build
        cp ./_site ./var/www/es/site/
